// lib/config/app_constants.dart
// ALL sizes, spacing, asset paths — change once, updates everywhere

class AppConstants {
  AppConstants._();

  // ── Farmer info ───────────────────────────────────────────────────────────
  static const farmerName = 'Akaran';
  static const farmName = "Akaran's Farm";

  // ── Assets ────────────────────────────────────────────────────────────────
  static const bgImage = 'assets/images/farm_bg2.jpeg';

  // ── Card sizes ────────────────────────────────────────────────────────────
  static const cardRadius = 22.0;
  static const clockRadius = 30.0;
  static const chipRadius = 20.0;
  static const iconBoxRadius = 16.0;

  // ── Spacing ───────────────────────────────────────────────────────────────
  static const screenPadding = 18.0;
  static const gridSpacing = 14.0;
  static const sectionGap = 28.0;

  // ── Grid ──────────────────────────────────────────────────────────────────
  static const gridColumns = 2;
  static const gridAspectRatio = 0.88;
}
